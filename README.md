# AI App Builder

## Introduction
This project is an AI App Builder System that generates fully-runnable applications from prompts.

## Features
- AI-driven app generation
- Customizable templates

## Installation
1. Clone the repository.
2. Run `npm install` to install dependencies.

## Usage
Run `npm start` to start the server and use the `/generate-app` endpoint to generate apps.
